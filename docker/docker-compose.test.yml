services:
  regression:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        PG_MAJOR: ${PG_MAJOR:-18}
    environment:
      PG_MAJOR: ${PG_MAJOR:-18}
      REPO_DIR: /workspace
    working_dir: /workspace
    volumes:
      - ..:/workspace
